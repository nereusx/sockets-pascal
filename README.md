# Pascal sockets server/client

Server (multithreaded) and client for command-line PASCAL.
Instead of sock-client you can also use `telnet localhost 4096`.

Commands to server
```
hello	replies hello
quit	closes the current session
down	shutdown the server
```
